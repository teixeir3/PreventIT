<%= render "doctors/sub_bar", doctor: @doctor%>

<main class="group">
  <div class="side-content">
    <div class="title">
      <h3>Appointment Types:</h3>
    </div>

    <div class="side-info">
		<ul>
			<li><%= link_to "Add New", new_appointment_type_url %></li>
		</ul>
	</div>
  </div>


  <div class="sub-content">
    <ul>
      <% @appointment_types.each do |appointment_type| %>
        <li><%= link_to appointment_type.name, edit_appointment_type_url(appointment_type) %></li>
      <% end %>
    </ul>
  </div>

</main>