<!DOCTYPE html>
<html>
<head>
  <title>PreventIT</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= filepicker_js_include_tag %>
  <%= csrf_meta_tags %>
  <link rel="shortcut icon" href="/favicon.ico?v=2" />
  <%= favicon_link_tag %>
  
  
</head>
<body>
  <header>
    <div class="top-bar group">
      <div class="logo">
        <img src="/assets/preventIT_final/preventIT_150x100_NG.png">
      </div>
      <div class="tagline">
        <h1>Chronic Health Management</h1>
      </div>

      <div class="nav-sign-ins">
        
         <%= flash[:errors].join('<br>').html_safe if flash[:errors] %><br>
         <% if current_user %>
           Welcome:
           <% if current_user.is_doctor  %>
             <%= link_to "Dr. #{current_user.full_name}", doctor_url(current_user) %><br>
           <% else %>
             <%= link_to "#{current_user.full_name}", user_url(current_user) %>
           <% end %>
           <ul>
             <li><%= button_to "Sign Out", session_url, :method => :delete, class: "button green-button" %></li>
             <li> <% settings_class = (params[:controller] == "users" && params[:action] == "edit") ? "green-settings-icon" : "settings-icon" %> <%= link_to "Settings", edit_user_url(current_user), class: settings_class %></li>
           </ul>
         <% else %>
           | <%= link_to "New Doctor", new_doctor_url%> |
           <%= link_to "New Patient", new_user_url%> |
           <%= link_to "Sign In", new_session_url %> |
         <% end %>
      </div>
    </div>

  </header>
  
  <div class="errors">
    <%= flash[:errors].join('<br>').html_safe if flash[:errors] %><br>
  </div>
  <div class="notices">
    <%= flash[:notices].join('<br>').html_safe if flash[:notices] %><br>
  </div>
  
    <div class="spinner"><%= image_tag "spinner-green.gif" %></div>
    
    <%= yield %>
    
    <%= yield :js %>
    


</body>

<footer class="group">
  <div class="copyright">
    <small>© Ayos Informatics, LLC 2014</small>
  </div>
  
  <div class="params group">
    <%  if Rails.env.development? %>
      <%= debug(params) %>
      <pre>This page was loaded on <strong><%=l Time.now, format: "%k:%M:%S:%L" %></strong></pre>
    <% end %>
  </div>
</footer>
</html>


