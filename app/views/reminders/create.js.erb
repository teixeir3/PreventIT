<% if @reminder.persisted? %>
  $('#reminders').append("<%= escape_javascript(render 'reminders/li', user: @user, reminder: @reminder) %>");
<% end %>

var flashError = "<%= flash[:errors].join('<br>').html_safe if flash[:errors] %>";
var flashNotice = "<%= flash[:notices].join('<br>').html_safe if flash[:notices] %>";
window.modal.hide();
