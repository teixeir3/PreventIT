<h2>Reminders:</h2>
<ul>
  <% @reminders.each do |reminder| %>
    <li><%=  "#{reminder.title} :  #{reminder.datetime.to_formatted_s(:time)} : #{reminder.datetime.to_formatted_s(:day)}" %>  </li>
  <% end %>
</ul>

<%= link_to "Add New Reminder", new_user_reminder_url(@user) %>